# This Gemfile is used to test the gem using the oldest supported dependecies
# specified in the gemspec file.

eval(IO.read("./Gemfile"), binding) # include everything in the main `Gemfile`

gem "builder", "2.1.1"
gem "nokogiri", "1.5.4"
gem "activesupport", "3.0.0"
gem "i18n", "0.2.0"
gem "httpclient", "2.5.2"
gem "addressable", "2.3.6"
gem "json", "1.8.2"